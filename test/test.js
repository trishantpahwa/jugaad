const assert = require("assert");
const { describe, it } = require("mocha");
const axios = require("axios");

describe("Test the APIs of the web application.", () => {
  it("PUT /contributor", () => {
    assert.equal(1, 1);
  });
  it("GET /contributors", () => {
    assert.equal(1, 1);
  });
  it("PUT /project", () => {
    assert.equal(1, 1);
  });
  it("GET /projects", () => {
    assert.equal(1, 1);
  });
});

// Add test cases => TP | 2024-06-13 21:26:58
